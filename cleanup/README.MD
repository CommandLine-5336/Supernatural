# Cleanup service
## Requirements
* docker v29.5.3
## Steps to run
* Create a `.env` file and fill it with necessary information, reference to `.env.example`
* After creating and filling `.env` run `docker compose up --build`, if it is your first time building the image, otherwise run `docker compose up`
