info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8080/login?username=johndoe&password=strong-password
  params:
    - name: username
      value: johndoe
      type: query
    - name: password
      value: strong-password
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
