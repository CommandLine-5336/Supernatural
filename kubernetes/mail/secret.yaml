apiVersion: v1
kind: Secret
metadata:
  name: mail-secret
  namespace: app
type: Opaque
stringData:
  DSN:  'host={RDS_PORT} port=5432 user={EXAMPLE} password={EXAMPLE} dbname={EXAMPLE} sslmode=require'
  EMAIL_FROM: '{EXAMPLE}'
  EMAIL_PASSWORD: '{EXAMPLE}'
  EMAIL_PORT: '{EXAMPLE}'
