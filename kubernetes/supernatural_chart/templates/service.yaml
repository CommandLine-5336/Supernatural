{{- range .Values.services  }}
{{- if .hasService}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .nameService }}
  namespace: app
spec:
  selector:
    app: {{ .appLabel }}
  ports:
  - port: {{ .containerPort }}
    targetPort: {{ .containerPort }}
  type: ClusterIP
{{- end }}
{{- end }}
